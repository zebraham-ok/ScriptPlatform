# 后续开发计划

> 更新日期：2026-06-22
> 分析范围：当前 backend/ + frontend/ 实现状态，以及玩家实际体验反馈

---

## 一、已实现功能（截至 2026-06-22）

以下功能不再列入后续计划：

| 模块 | 说明 |
|------|------|
| Vite 构建 + TailwindCSS 4 + Ant Design 双风格 | ✅ |
| LangGraph 10 节点状态机 | ✅ |
| Socket.IO 实时通信 (20+ 事件) | ✅ |
| RESTful API (`/api/*`) | ✅ |
| 三种游戏模式 (沙盒/剧本/导入) | ✅ |
| 广场剧本浏览/筛选/发布/导入 | ✅ |
| 角色选择 + 角色卡编辑 + 属性约束校验 | ✅ |
| 多玩家轮次制 + 倒计时 + 跳过/延长 | ✅ |
| DM 叙述 + 私密消息 + 快捷选项 | ✅ |
| 检定/骰子 + 投票机制 | ✅ |
| 场景图 AI 生成 + 缓存 | ✅ |
| TTS 语音合成 (EdgeTTS，纯文本模式) | ✅ |
| 长期记忆 (初始化 + 每轮更新) | ✅ |
| 结局卡片 + 多结局分支 | ✅ |
| 游客模式 + 房间分享链接 | ✅ |
| 暗色游戏主题 | ✅ |
| BGM 背景音乐（编辑器选曲 → 游戏内 `<audio>` 循环播放，音量可调） | ✅ |
| 开场词附带可点击选项（填入输入框后可编辑再发送） | ✅ |
| 编辑器导入/导出 (JSON/LangGraph/Python) | ✅ |
| 编辑器试玩（导出 → 创建房间 → 跳转游戏） | ✅ |
| 开场白强化（起始节点场景把控 + 玩家角色身份注入） | ✅ |
| 物品/地点 label 同步 + 缺失字段补全 | ✅ |
| worldParams 固定值在角色卡提交时保留 | ✅ |
| 服务器部署（Nginx + systemd + 单 worker） | ✅ |

---

## 二、优先需求（来自实际体验反馈）

### 🔴 Tier 1 — 编辑器核心体验

#### 1. 剧本编辑平台的详细说明指引

> **问题**：新创作者面对编辑器的 6 个页面（人物/地点/物品/世界观/情节树/功能）不清楚从何下手，不知道每个字段的用途，不知道"可扮演角色""起始节点""结束节点"等概念。
>
> **建议**：
> - 每个编辑页面添加 **"?"帮助按钮**，点击弹出该页面的功能说明
> - 首次进入编辑器时显示**步骤引导**（Stepper 或 Tour 组件）：① 世界观设定 → ② 人物创建 → ③ 地点/物品 → ④ 情节树 → ⑤ 发布
> - 提供**示例剧本模板**，新用户可直接导入参考
> - 在"情节树"页面给出**节点连线示例**（谁是起始节点、结束节点如何标记）

**预估工时**：6 小时

---

#### 2. 撤销错误操作 (Undo/Redo)

> **问题**：创作者在编辑时如果误删节点、改错字段、错误的连线操作，无法撤销，只能手动恢复。这是编辑器的基本功能缺失。
>
> **建议**：
> - 在项目 Store 中实现 **操作历史栈**（command pattern 或 state snapshot）
> - 支持 `Ctrl+Z` 撤销、`Ctrl+Y` 重做
> - 工具栏显示撤销/重做按钮
> - 历史深度至少 50 步
> - 切换编辑页面时不清除历史

**预估工时**：8 小时

---

#### 3. 发布的剧本可以撤回

> **问题**：剧本发布到广场后，创作者发现还需修改，但无法撤回。只能在广场留下旧版本，再发布新版（会产生多个版本）。
>
> **建议**：
> - 广场剧本列表增加 **"撤回"按钮**（仅作者可见）
> - 撤回后剧本从广场下架，但保留在创作者的本地项目中
> - 修改后可重新发布（覆盖旧版或作为新版）
> - "我的发布"管理页面，集中管理已发布剧本

**预估工时**：4 小时

---

#### 4. 创作者上传自定义音乐

> **问题**：当前 BGM 只能从预设的音乐列表中选择。创作者希望能上传自己的音乐文件作为剧本专属 BGM。
>
> **建议**：
> - 编辑器 BGM 选择器增加 **"上传"入口**
> - 后端新增 `POST /api/music/upload` 端点（接受 `.mp3/.wav/.ogg`，限制大小如 20MB）
> - 上传后自动加入当前用户的音乐列表
> - 区分"公共音乐"和"我的上传"
> - 发布剧本时，若使用了自定义音乐，一并打包（或在服务器上保持可访问）

**预估工时**：5 小时

---

#### 5. 更强的 AI 生成能力（直接增加节点、填写内容）

> **问题**：当前 AI 辅助主要在做对话式回答。创作者期望 AI 能 **直接操作编辑器数据**：根据世界观描述自动创建人物节点、生成情节节点并连线、批量填充地点/物品数据。
>
> **建议**：
> - **AI 自动创建人物**：输入"江湖门派"，AI 生成掌门、弟子、护法等角色节点（含属性/背景/动机）并插入人物图
> - **AI 自动构建情节**：输入故事梗概，AI 输出完整情节树（节点 + 连线 + 条件分支）
> - **AI 批量填充**：对已创建的节点骨架，AI 根据世界观自动填充描述、对话、背景
> - 每次 AI 操作前**预览变更**（Diff 视图），创作者确认后应用
> - 与撤销功能配合：AI 操作作为可撤销的一步

**预估工时**：12 小时

---

### 🟡 Tier 2 — DM 主持人智能提升

#### 6. DM 情节走向掌控能力增强

> **问题**：当前 DM 根据玩家发言生成回复，但对**整体情节节奏**的控制力较弱。DM 容易在细节上打转（反复描述同一场景），或跳过关键情节节点直奔结局。
>
> **建议**：
> - 在 `dm_response_node` 的 system prompt 中注入**当前所处情节节点**的详细信息（该节点的目标、条件、推荐行动方向）
> - 增加**情节进度追踪**：DM 在每一轮回复后，更新"当前进度"（已完成/进行中/未开始），引导向下一个节点推进
> - 当检测到玩家行为触发节点条件时，DM **主动推进剧情**（而非等待玩家自己发现线索）
> - 支持 **DM 主动干预指令**（dm_action 事件）：强制推进到指定节点、触发特定事件、引入 NPC

**预估工时**：8 小时

---

#### 7. DM 上下文鉴别能力提升

> **问题**：当前 DM 的 AI prompt 中注入了大量上下文（人物/地点/物品/情节/长期记忆/历史消息），但**没有优先级或筛选机制**。结果是：
> - 不相关的人物/地点信息干扰 DM 判断
> - 过长的历史消息导致有效上下文被截断
> - DM 不知道哪些信息对当前场景是重要的
>
> **建议**：
> - 在 `context_builder.py` 中实现**上下文相关性排序**：
>   - 根据当前场景地点，优先注入该地点的描述和相关人物
>   - 根据当前情节节点，优先注入该节点的条件和目标
>   - 与当前场景无关的物品/人物降低权重或省略
> - **历史消息智能摘要**：用 AI 对历史对话进行压缩（保留关键决策和情节转折点），而非原文全量注入
> - 在 DM prompt 中明确标注 **"当前关键信息"** 和 **"背景参考信息"** 两档，让 DM 区分主次
> - 上下文 token 预算管理：确保系统 prompt + 上下文不超过模型限制的 70%，预留空间给 DM 回复

**预估工时**：10 小时

---

### 🟢 Tier 3 — 沉浸体验增强

#### 8. 打字机逐字显示效果

> **来源**：BUMENGweb-main 中的 DM 消息渲染

BUMENGweb-main 的 DM 叙述使用**逐字显示 + 闪烁光标**效果。当前 `ChatPanel.tsx` 直接展示完整文本。

**建议**：
- 在 `ChatPanel.tsx` 中为 `role="dm"` 的消息添加 Typewriter 组件
- 支持点击跳过直接显示全文
- 与 TTS 播报进度同步（可选）

**预估工时**：3 小时

---

#### 9. Canvas 2D 粒子背景动效

> **来源**：BUMENGweb-main 中 Canvas 粒子系统

BUMENGweb-main 有 30 个粒子 + 连线的动态 Canvas 背景。在 PlazaPage 和 LobbyPage 中使用（游戏页面有场景图，不需要）。

**预估工时**：3 小时

---

### 🟢 Tier 4 — 系统健壮性

#### 10. 断线重连与房主自动转移

> **来源**：BUMENGweb-main 重连缓冲

**建议**：
- `disconnect` 事件中增加 30 秒宽限期，而非立即移除玩家
- 添加 `reconnect_room` Socket.IO 事件
- 房主断线时自动转移给在线时间最长的玩家
- 全部玩家断线后保留房间一段时间

**预估工时**：4 小时

---

#### 11. 玩家活跃度监控 + DM 自动干预

> **来源**：BUMENGweb-main 后台监控

**建议**：
- 在 `game_server.py` 中添加后台协程
- 5 分钟无玩家行动 → DM 自动环境描写或私聊催促
- 在 `player_action` 时重置计时器

**预估工时**：4 小时

---

#### 12. 道具图片 AI 生成

> **来源**：BUMENGweb-main 中的道具图生成

当前平台只有场景图生成，道具获得后仅有文字通知。

**建议**：
- 在 `image_service.py` 中新增 `generate_item_image()` 函数
- 在 `game_server.py` 的 `character_update` 事件检测到新道具时触发生成

**预估工时**：3 小时

---

#### 13. 结局卡片导出为图片

> **来源**：BUMENGweb-main 使用 `html2canvas`

当前 `EndingCard.tsx` 仅显示卡片，无导出功能。`package.json` 中已有 `html2canvas` 依赖但未使用。

**建议**：
- 在 `EndingCard.tsx` 中添加"导出图片"按钮
- 使用 `html2canvas` 截图卡片 DOM 并触发下载

**预估工时**：2 小时

---

### 🔵 Tier 5 — 平台生态

#### 14. LangGraph Checkpoint 暂停/恢复

> **来源**：GAME_INTEGRATION_PLAN.md

LangGraph 内置 `MemorySaver` Checkpoint 机制未利用。

**建议**：
- 设计 checkpoint 持久化方案
- 支持 `pause_game` / `resume_game` 事件
- 玩家全部离开后保存 checkpoint，可恢复游戏

**预估工时**：5 小时

---

#### 15. 文学语义搜索集成

> **来源**：BUMENGweb-main 中的 905 部文学作品语义索引

**建议**：
- 迁移 `literature_search.py` 和 embeddings 到 backend
- 在 AI prompt 中注入相似文学作品参考

**预估工时**：5 小时

---

#### 16. 数据存储升级（文件系统 → SQLite）

> **来源**：GAME_INTEGRATION_PLAN.md

文件系统 JSON 不够支撑评分/评论/收藏功能。

**建议**：
- 引入 SQLite + SQLAlchemy
- 新增 `ratings`、`comments`、`favorites`、`play_history` 表
- 文件系统 + DB 双写过渡期

**预估工时**：12 小时

---

#### 17. 深度剧情推演增强

> **来源**：BUMENGweb-main 中的导演手册生成

**建议**：
- 在 `json_load_node` 中增加 AI 深度推演（主线逻辑推导、多结局路径、节奏控制、高光时刻设计）
- 作为"高级开局"选项

**预估工时**：4 小时

---

## 三、汇总对比

### 3.1 BUMENGweb-main 功能覆盖

| 功能 | 当前状态 |
|------|----------|
| 多人房间系统 | ✅ 完整 |
| AI 剧本生成 | ✅ 完整 |
| 回合制推进 + DM 叙述 | ✅ 完整 |
| 角色卡系统 | ✅ 完整 |
| 场景图 AI 生成 | ✅ 完整 |
| DM 语音播报 (TTS) | ✅ 完整（纯文本模式） |
| 骰子判定 + 投票 | ✅ 完整 |
| 公屏 + 私密消息 | ✅ 完整 |
| 长期记忆 | ✅ 完整 |
| BGM 音乐系统 | ✅ 完整（文件播放模式） |
| 打字机逐字效果 | ❌ 未实现 |
| Canvas 粒子背景 | ❌ 未实现 |
| 文学参考检索 | ❌ 未实现 |
| 活跃度监控 | ❌ 未实现 |
| 道具图生成 | ❌ 未实现 |
| 结局卡片导出 | ❌ 未实现 |
| 断线重连 30s 宽限 | ❌ 未实现 |
| 房主自动转移 | ❌ 未实现 |

### 3.2 编辑器核心能力

| 功能 | 当前状态 |
|------|----------|
| 6 个编辑页面（人物/地点/物品/世界观/情节树/功能） | ✅ |
| JSON 导入/导出 | ✅ |
| 编辑器试玩 | ✅ |
| BGM 选曲 | ✅ |
| 操作说明指引 | ❌ 未实现 |
| 撤销/重做 (Undo/Redo) | ❌ 未实现 |
| 发布撤回 | ❌ 未实现 |
| 自定义音乐上传 | ❌ 未实现 |
| AI 直接操作编辑器数据（增删节点/填充内容） | ❌ 未实现 |

### 3.3 DM 智能水平

| 功能 | 当前状态 |
|------|----------|
| 根据玩家发言 + 上下文生成 DM 回复 | ✅ |
| DM 快捷选项（可点击填入输入框） | ✅ |
| DM 对情节进度的主动掌控 | ⚠️ 弱 |
| 上下文相关性筛选/优先级排序 | ⚠️ 无 |
| 历史消息智能摘要 | ⚠️ 原文全量注入 |
| 上下文 token 预算管理 | ⚠️ 无 |

---

## 四、推荐行动路线

### 第一优先级：编辑器核心体验（约 35 小时）

对创作者最直接的价值提升：

| 序号 | 任务 | 预估 | 理由 |
|------|------|------|------|
| 1 | 操作说明指引 | 6h | 降低新用户上手门槛 |
| 2 | 撤销/重做 | 8h | 编辑器基本功能，频繁被需要 |
| 3 | 发布撤回 | 4h | 创作者自主权的基本保障 |
| 4 | AI 直接操作编辑器 | 12h | 编辑效率成倍提升，核心竞争力 |
| 5 | 自定义音乐上传 | 5h | 差异化创作需求 |

### 第二优先级：DM 智能提升（约 18 小时）

直接影响游戏体验质量：

| 序号 | 任务 | 预估 | 理由 |
|------|------|------|------|
| 6 | 情节走向掌控 | 8h | 解决"DM 打转"问题 |
| 7 | 上下文鉴别能力 | 10h | 提升 DM 回复质量，减少无关信息干扰 |

### 第三优先级：沉浸体验（约 12 小时）

| 序号 | 任务 | 预估 |
|------|------|------|
| 8 | 打字机逐字显示 | 3h |
| 9 | 粒子背景动效 | 3h |
| 10 | 道具图片生成 | 3h |
| 11 | 结局卡片导出 | 2h |
| 12 | 活跃度监控 | 4h |

### 第四优先级：系统健壮性 + 平台生态（约 30 小时）

| 序号 | 任务 | 预估 |
|------|------|------|
| 13 | 断线重连 + 房主转移 | 4h |
| 14 | LangGraph Checkpoint | 5h |
| 15 | 文学语义搜索 | 5h |
| 16 | 深度剧情推演增强 | 4h |
| 17 | 数据存储升级 | 12h |

---

## 五、风险提示

| 风险 | 说明 | 应对 |
|------|------|------|
| Undo/Redo 实现复杂度 | 涉及 6 个编辑页面的多种操作类型（节点 CRUD、连线、字段编辑） | 采用 state snapshot 方式（全量快照）而非 command pattern，降低实现复杂度 |
| AI 直接编辑可能破坏数据结构 | AI 生成的节点/连线可能不符合 schema 约束 | AI 操作后必须通过 Pydantic 校验，失败时回滚并提示用户 |
| 上下文鉴别依赖额外 AI 调用 | 摘要和筛选需要调 AI，增加延迟和成本 | 优先用规则（地点匹配、节点条件匹配）做粗筛，AI 摘要作为可选增强 |
| 音乐上传的存储和版权 | 用户上传的音乐文件可能侵权 | 仅限个人使用，不上传到公共库；提示用户版权风险 |
| 发布撤回后已有玩家在游玩 | 剧本已被其他人加入房间 | 撤回不中断当前正在进行的游戏，仅阻止新玩家加入 |

---

## 六、预估总工时汇总

| 优先级 | 任务数 | 预估工时 |
|--------|--------|----------|
| 第一优先级（编辑器核心） | 5 | 35h |
| 第二优先级（DM 智能） | 2 | 18h |
| 第三优先级（沉浸体验） | 5 | 15h |
| 第四优先级（健壮+生态） | 5 | 30h |
| **总计** | **17** | **约 98 小时** |

---

*计划基于 2026-06-22 代码库状态编制。建议每完成一个优先级的任务后重新评估剩余工时。*
