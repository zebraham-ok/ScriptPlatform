
# 在线剧本编辑平台 - 项目完整说明

请根据以下需求，生成一个完整的在线剧本编辑平台项目代码。项目需要包含前端（React + TypeScript）和后端（Python FastAPI），后端使用文件系统存储 JSON 项目文件（无数据库）。平台允许创作者通过可视化的方式编辑剧本要素，并利用大模型辅助创作。最终可将项目导出为标准化 JSON，直接用作 LangGraph Agent 的初始状态。

---

## 1. 技术栈

### 前端
- React 18+、TypeScript
- UI 框架：Ant Design（布局、表单、按钮、弹窗等）
- 状态管理：Zustand（全局存储项目数据、UI 状态）
- 图可视化编辑：React Flow（用于人物关系图、地点图、情节树的有向图）
- HTTP 客户端：Axios
- 路由：无需 react-router，单页通过 Tab 切换子页面

### 后端
- Python 3.10+、FastAPI
- 大模型调用：openai 库（可扩展其他 LLM）
- 文件存储：每个项目保存为一个 JSON 文件，存放在服务端 `./projects/` 目录下
- 用户认证：简化实现，使用固定的用户 ID（比如 `user1`），暂不实现注册登录（后续可扩展 JWT）
- CORS 中间件允许前端跨域

### 存储
- 后端 `./projects/` 文件夹，每个用户下按 `{user_id}/` 分隔，每个项目文件名为 `{project_id}.json`
- 文件内容为完整的项目 JSON（包含所有节点、边、世界观文本、AI 配置等）

---

## 2. 项目结构

```
script-builder/
├── frontend/               # React 前端项目
│   ├── public/
│   ├── src/
│   │   ├── api/            # Axios 实例与后端接口调用
│   │   ├── components/     # 可复用组件
│   │   │   ├── Layout/     # 总体布局（Header、Tab切换、导出按钮）
│   │   │   ├── GraphCanvas/ # React Flow 画布封装
│   │   │   ├── DetailPanel/ # 右侧详情编辑面板（根据选中元素动态表单）
│   │   │   └── AIPanel/    # AI 助手侧边栏或弹窗
│   │   ├── pages/          # 各子页面
│   │   │   ├── CharacterPage.tsx    # A 人物
│   │   │   ├── LocationPage.tsx     # B 地图
│   │   │   ├── WorldviewPage.tsx    # C 世界观
│   │   │   └── PlotPage.tsx         # D 情节树
│   │   ├── store/          # Zustand stores（项目数据、UI状态）
│   │   ├── types/          # TypeScript 类型定义
│   │   └── utils/          # 工具函数（导出、验证等）
│   └── package.json
├── backend/                # FastAPI 后端
│   ├── main.py             # 应用入口、路由定义
│   ├── routers/
│   │   ├── projects.py     # 项目 CRUD 接口
│   │   └── ai.py           # AI 生成接口
│   ├── services/
│   │   ├── file_store.py   # 文件系统存储服务
│   │   └── ai_service.py   # LLM 调用逻辑、提示词模板
│   ├── models/             # Pydantic 模型定义
│   │   └── schemas.py      # 项目数据模型
│   ├── projects/           # 存放项目 JSON 文件的目录
│   └── requirements.txt
└── README.md
```

---

## 3. 页面布局与交互

### 整体布局
- **顶部栏**：
  - 左上角：四个子页面切换 Tab（人物 / 地图 / 世界观 / 情节），当前选中高亮。
  - 右上角：导出 JSON 按钮（点击后弹出导出选项，可导出"完整项目"或"LangGraph State"）。
- **主内容区**：
  - 若当前页为 A、B、D：左右分栏（可通过 Ant Design 的 Splitter 实现）。
    - 左侧：图可视化画布（React Flow），宽度占 60%~70%。
    - 右侧：详情编辑栏（根据点击的节点或边动态显示属性表单），宽度占 30%~40%。
  - 若当前页为 C（世界观）：占满整个主内容区，提供多段落文本编辑（标题+内容），或一个 Markdown 编辑器。

### 图编辑通用交互（A、B、D）
- **画布操作**：可缩放、平移、拖拽节点。
- **创建节点**：画布上方工具栏提供"添加"按钮，或从侧边拖入预设节点类型。
  - 人物图：节点类型固定为"人物"。
  - 地点图：节点类型固定为"地点"。
  - 情节树：节点类型固定为"检查点/状态"。
- **创建边**：从一个节点的 Handle 拖拽到另一个节点的 Handle，生成连线。边支持设置标签和属性。
- **选中元素**：点击节点或边后，右侧编辑栏显示其详细属性表单。
- **自动保存**：编辑操作通过防抖自动调用后端保存接口（PATCH），保证数据持久化。

### 右侧详情编辑栏
根据不同节点类型或边显示不同的表单字段，所有字段更新会立即反映到 Zustand store 并触发自动保存。每个文本属性右侧有 AI 填充按钮（🤖），点击后 AI 根据完整项目上下文智能填充该字段。
- **通用**：节点标签、自定义描述。
- **人物节点**：姓名、别名、性别、年龄、外貌、性格、核心动机、初始位置（下拉选择地点）、自定义属性键值对（支持添加/删除）。
- **地点节点**：名称、类型（城市/建筑/自然）、描述、地貌、自定义属性。
- **物品节点**：物品名称、外观、功能、获得方式、初始所在（下拉绑定）、触发事件列表。
- **情节节点**：检查点名称、设为结局/设为起始切换、告知玩家信息、不告知玩家信息、潜在行动列表、事件绑定（地点/物品/人物多选）。
- **边**：关系/路径/转化标签、详细说明、条件（仅情节转化）。

### 世界观测览页（C）
- 提供多个文本块编辑，每个块包含：标题（如"历史背景"）和内容（富文本或 Markdown）。
- 支持增加/删除文本块。
- 数据存储在项目 JSON 的 `worldSetting` 数组中。

---

## 4. 数据模型（标准化 JSON 结构）

整个项目在前后端使用统一的 JSON 对象。该对象在后端保存为文件，在前端 Zustand 中维护。

```typescript
// TypeScript 接口定义（前端）
interface ProjectData {
  projectId: string;
  title: string;
  worldSetting: WorldBlock[]; // 世界观文本块数组
  characters: GraphData;      // 包含 nodes 和 edges
  locations: GraphData;
  plot: PlotData;
  // 可选 AI 配置
  aiConfig?: AIConfig;
}

interface WorldBlock {
  id: string;
  title: string;
  content: string; // 支持 Markdown
}

interface GraphData {
  nodes: NodeData[];
  edges: EdgeData[];
}

interface NodeData {
  id: string;            // 唯一 ID
  type: string;          // 'character' | 'location' | 'checkpoint'
  label: string;         // 显示名称
  position: { x: number; y: number };
  data: {
    // 人物特有
    name?: string;
    aliases?: string[];
    gender?: string;
    attributes?: Record<string, any>;   // 自定义键值对
    description?: string;
    // 地点特有
    locationType?: string;
    // 情节检查点特有
    sceneDescription?: string;
    conditions?: string[];
    // 通用
    customFields?: Record<string, any>; // 用户扩展的任意属性
  };
}

interface EdgeData {
  id: string;
  source: string;        // 源节点 ID
  target: string;        // 目标节点 ID
  label: string;         // 关系/转化名
  data: {
    description?: string;
    // 情节特有
    trigger?: string;
    conditionLogic?: string; // 如 "has_item('地图')"
    // 自定义
    customFields?: Record<string, any>;
  };
}

interface PlotData {
  initialCheckpoint: string; // 起始节点 ID
  graph: GraphData;          // 节点与转换边（节点 type 为 'checkpoint'）
}

interface AIConfig {
  apiKey?: string; // 如果前端允许配置，可加密存储；建议放后端环境变量
  model: string;   // 例如 "gpt-4"
}
```

**关键约定**：
- 节点中冗余的 `relationships` 不单独存储，关系由 `edges` 定义。
- 所有自定义键值对放入 `customFields`，保证导出 JSON 结构统一。

---

## 5. 后端 API 设计

Base URL：`http://localhost:8000/api`

所有接口暂不校验用户身份，假定用户 ID 为 `user1`（前端固定）。

### 5.1 项目管理
- `GET /api/projects` - 获取项目列表（返回每个项目的 id、title、updatedAt 摘要）
- `POST /api/projects` - 创建新项目（请求 body：{ title: string }），返回完整项目数据（含初始化空图）
- `GET /api/projects/{project_id}` - 获取单个项目完整数据
- `PUT /api/projects/{project_id}` - 全量更新项目数据（前端定期保存时调用）
- `PATCH /api/projects/{project_id}` - 部分更新项目数据（前端自动保存时建议用此方法，只传变更字段）
- `DELETE /api/projects/{project_id}` - 删除项目

### 5.2 AI 辅助
- `POST /api/ai/generate` - 根据上下文和指令生成内容
  - 请求 body：
    ```json
    {
      "project_id": "...",
      "context": {
        "current_page": "character" | "location" | "plot" | "worldview",
        "selected_element_id": "可选",
        "nearby_elements": []
      },
      "instruction": "请为这个角色创作一个背景故事",
      "prompt_template": "optional_custom_template"
    }
    ```
  - 响应：`{ generated_text: "..." }`，或支持流式响应（SSE），初期可用普通 JSON 响应。

- `POST /api/ai/fill-field` - AI 根据完整项目上下文智能填充单个字段
  - 请求 body：
    ```json
    {
      "project_id": "...",
      "field_name": "personality",
      "existing_content": "内向",
      "node_type": "character"
    }
    ```
  - 响应：`{ "analysis": "分析过程...", "content": "建议填充的内容" }`
  - 如果 `existing_content` 不为空，AI 会在现有内容基础上进行扩充和润色
  - 前端仅使用 `content` 字段填充表单，`analysis` 字段仅在控制台打印

后端 `ai_service.py` 内根据 `current_page` 和元素信息，拼接预设的提示词模板，调用 OpenAI/Qwen API，返回结果。提示词模板可让用户自定义，但系统提供默认模板。

### 5.3 文件存储逻辑 (`file_store.py`)
- 项目保存路径：`./projects/{user_id}/{project_id}.json`
- 初始化时确保目录存在。
- 读取：直接 `json.load`
- 写入：先备份旧文件？可选，简单覆盖。
- 列表接口：遍历目录，读取每个文件内的 title 和 updatedAt。

---

## 6. 大模型集成要求

- 后端使用 `openai` 库（支持 OpenAI 或任何兼容 API，如 Qwen DashScope）。
- API Key 从环境变量 `QWEN_API_KEY` / `OPENAI_API_KEY` 读取，不硬编码。
- 提供默认提示词模板（中英文），例如：
  - 角色生成：`你是一个剧本创作助手。根据以下已有的角色设定，为这个角色丰富背景故事。角色信息：{char_info}`
  - 情节建议：`当前剧情处于状态"{state}"，可能的下一步发展是？请提出3个合理的事件转化，并附带触发条件。`
  - 环境描写：`请为地点"{name}"写一段生动的环境描写，氛围：{atmosphere}。`
  - 字段填充：`你是一个专业的剧本创作助手。请根据项目的完整JSON数据，为指定字段填充或扩充内容。`
- 前端在右侧编辑栏或独立侧边栏放置"AI 助手"按钮，点击后弹出抽屉，用户输入指令，调用后端接口，展示返回结果。
- **字段级 AI 填充**：在详情编辑面板中，每个文本属性右侧增加 🤖 按钮，点击后异步调用 `fill-field` 接口。AI 返回的 `content` 直接填入表单字段，返回期间不影响其它操作。

---

## 7. 导出 JSON 功能

### 右上角导出按钮
点击后弹出下拉菜单：
- **导出完整项目**：下载包含画布坐标、UI 状态的完整 JSON（用于备份）。
- **导出 LangGraph State**：下载仅包含剧本结构化数据的 JSON，去除 `position`、UI 等无关字段，结构如下：

```json
{
  "script_title": "...",
  "world_setting": [ { "title": "...", "content": "..." } ],
  "characters": [
    {
      "id": "c1",
      "name": "张三",
      "attributes": { "age": 25 },
      "relationships": [ { "target": "c2", "type": "挚友", "desc": "..." } ]
    }
  ],
  "locations": [
    {
      "id": "l1",
      "name": "龙门客栈",
      "description": "...",
      "connections": [ { "target": "l2", "type": "道路", "desc": "..." } ]
    }
  ],
  "plot": {
    "initial_checkpoint": "p1",
    "checkpoints": {
      "p1": { "id": "p1", "label": "序幕", "description": "...", "conditions": [] },
      ...
    },
    "transitions": [
      { "from": "p1", "to": "p2", "trigger": "主角出发", "condition": "..." }
    ]
  },
  "metadata": {}
}
```

- **导出 Python 初始化代码**：生成一段 Python 代码，直接将 State 字典赋值给变量 `initial_state`，方便用户复制粘贴到 LangGraph 脚本。

导出逻辑在前端实现，从 Zustand store 中读取数据，根据选择格式转换后触发下载。

---

## 8. 详细页面设计

### 8.1 人物页面（A）
- 左侧画布：节点为人物，连线代表关系（朋友、敌对、亲人等）。连线可双击或右侧编辑修改标签。
- 右侧编辑：选中人物时显示姓名、别名、性别、外貌、性格、自定义属性列表（可增删键值对）。选中边时显示关系类型和描述。

### 8.2 地点页面（B）
- 左侧画布：节点为地点（城市、村镇、特殊建筑），连线表示路径或方位关系（"相邻""包含"等）。
- 右侧编辑：地点名称、类型（下拉：城市/野外/建筑）、描述、地貌、自定义属性。

### 8.3 世界观页面（C）
- 整页文本编辑：提供"添加模块"按钮，每个模块包含标题和 Markdown 编辑器（推荐 `react-md-editor` 或简单 textarea）。支持拖拽排序。

### 8.4 情节树页面（D）
- 左侧画布：节点为剧情检查点/状态，使用有向边（带箭头）表示情节转化。一条边从一个状态指向另一个状态，表示一个转化可能。
- 画布需提供一个"设置为起始"按钮（右键菜单或工具栏），标记某个节点为初始检查点（显示特殊样式）。
- 右侧编辑：检查点名称、场景描述、达成条件列表（可编辑字符串数组）。选中边时：转化名称（触发事件）、条件逻辑文本、描述。

---

## 9. 关键实现细节

### React Flow 配置
- 自定义节点组件：根据类型渲染不同颜色图标（人物用头像、地点用图钉、检查点用旗帜）。
- 自定义边组件：默认直线，情节树中需显示箭头。
- 启用 snap-to-grid、小地图（可选）。
- 提供工具栏：添加节点、删除选中、撤销/重做（Zustand temporal 中间件实现）。

### 状态管理
- 使用 Zustand 创建一个 `useProjectStore`，存储当前打开项目的完整 `ProjectData`、选中元素 ID、页面类型等。
- 持久化：前端不主动持久化，仅通过后端保存。但可使用 Zustand 的 `persist` 存入 localStorage 作为备份，在线时以后端为准。

### 自动保存
- 在 store 中订阅变化，使用 `debounce` 每隔 2 秒调用 `PATCH /api/projects/{id}` 发送差异数据（或全量数据，简单实现）。
- 项目初始加载后，将数据灌入 store。

### 右侧编辑面板联动
- 监听 React Flow 的 `onSelectionChange`，将被选中的节点或边 ID 存入 store。
- 右侧 `DetailPanel` 根据选中元素的类型渲染不同表单。
- 表单字段变化直接修改 store 中对应节点/边的属性，触发自动保存。

---

## 10. 开发步骤建议（供 CodeBuddy 参考）

1. **后端初始化**：FastAPI 项目骨架，文件存储服务，项目 CRUD 接口，测试保存/读取 JSON 文件。
2. **前端脚手架**：React + TypeScript + Ant Design 布局，Zustand 空 store，路由 Tabs。
3. **前端项目加载与保存**：项目列表页（或直接通过下拉选择），加载项目数据到 store，自动保存测试。
4. **图页面实现**：先实现一个通用图页面组件，集成 React Flow，完成节点/边的渲染与增删。
5. **右侧编辑面板**：根据选中元素动态表单，修改 store 数据。
6. **四个页面具体功能**：将通用图组件复用到人物、地点、情节，差异化处理右侧表单和节点样式。
7. **世界观页面**：独立实现文本块编辑。
8. **AI 集成**：后端 AI 路由，前端助手抽屉，调用并回填。
9. **导出功能**：实现三种导出格式，下载触发。
10. **美化与细节**：自定义节点样式、提示词模板优化、错误处理。

---

## 11. 交付要求

请生成完整的项目代码，包含所有必要的文件，确保：
- 前后端可独立启动（提供启动说明）。
- 前端能连接后端进行项目加载和保存。
- 各页面的图编辑、文本编辑功能正常工作。
- 导出功能可生成规定的 JSON 文件。
- AI 辅助功能需给出可用的默认提示词模板。
